/*
  Backup of the corrupted SignupPage.java that was causing compile errors.
  The original file contained duplicated package/import/class blocks and syntax errors.
  If you need the full original bytes, retrieve from your local Git history or the workspace before this change.

  File moved to this backup to remove it from the test compile path.
*/
package com.example.pages;

public class SignupPage_corrupt_backup {
    // This file intentionally contains no executable code.
}
